-- The SELCT STATEMENT is used to select data from database. and returned data is stored in result table. called the result set.

-- SELECT DISTINCT: This statement is used to select only distinct values.
    -- Eg:- SELECT DISTINCT City FROM Customers;
    -- SELECT COUNT(DISTINCT Country) FROM Customers; (Returns the count of distinct countries)

