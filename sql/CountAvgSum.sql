-- COUNT() returns the number of rows that matches the specified criteria

-- COUNT();
    -- The following SQL statement finds the number of products:
    -- SELECT COUNT(ProductID) FROM Products.

-- AVG();
    -- The following statement finds the avg price of all the products.
    -- SELECT AVG(Price) FROM Products.

-- SUM();
    -- The following statements returns the sum of the `Quantity` feild FROM OrderDetails Tables. 
    -- SELECT SUM(Quantity) FROM OrderDetails.
    