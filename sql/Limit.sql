-- LIMIT Clause is used to LIMIT the number of records to return.

-- Syntax
    -- SELECT CustomerName, ContactName FROM Customers WHERE CustomerID = 3 LIMIT 10;
    -- SELECT * FORM Customers LIMIT 10; 