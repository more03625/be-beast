// const get = (array: number[], index: number): string => {
//     if (index >= 0 && index < array.length) {
//         return `Value present at index ${index} is ${array[index]}`;
//     }
//     return `Invalid Index`;
// };
// const set = (array: number[], valueToSetAtIndex: number, valueToSet: number): string => {
//     if (valueToSetAtIndex >= 0 && valueToSetAtIndex < array.length) {
//         array[valueToSetAtIndex] = valueToSet;
//         return `Here is your new array [${array}]`;
//     }
//     return `Invalid Index`;
// };
// const max = (array: number[]): string => {
//     // Time Complexity: O(n)
//     if (array.length > 0) {
//         let maxElement = array[0];
//         for (let i = 1; i < array.length; i++) {
//             if (array[i] > maxElement) {
//                 maxElement = array[i];
//             }
//         }
//         return `Maximum element from the array is ${maxElement}`;
//     } else {
//         return 'There are no elements in the array!';
//     }
// };
// const avg = (array: number[]): number => {
//     const numberOfElements = array.length;
//     let total = 0;
//     for (let i = 0; i < array.length; i++) {
//         total = total + array[i];
//     }
//     const avgNumber = total / numberOfElements;
//     return avgNumber;
// };
// const array: number[] = [4, 8, 10, 15, 18, 21, 24, 27, 29, 33, 34, 37, 39, 41, 43];
// const indexToGet: number = -1;
// const valueToSetAtIndex: number = 5;
// const valueToSet: number = 20;
// // Uncomment the following lines to test the functions
// // console.log(get(array, indexToGet));
// // console.log(set(array, valueToSetAtIndex, valueToSet));
// // console.log(max(array));
// // console.log(avg(array));
console.log('RAHUL MORE');
